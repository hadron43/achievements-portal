import * as ActionTypes from './actionTypes';

export const Records = (state = {
        isLoading: false,
        errMess: null,
        updates: {}
    }, action) => {
    switch(action.type) {
        default:
            return state;
    }
}